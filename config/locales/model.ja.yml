# モデルは全て activerecord 以下に記述する。
# これにより、User.model_name.human / User.human_attribute_name({attr_name})で使用可能。

ja:
  activerecord:
    models:
      identity: ユーザー
    attributes:
      identity:
        name: 氏名
        email: メールアドレス
  attributes:
    created_at: 作成日
    updated_at: 更新日
